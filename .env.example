# Environment Configuration
# Copy this file to .env and update with your values

# Set the environment (local, test, stage, prod)
ENV=local

# Override base URL if needed (otherwise uses env.ts defaults)
# BASE_URL=http://localhost:3000

# Test credentials (optional - tests will skip if not provided)
TEST_USERNAME=user@example.com
TEST_PASSWORD=password123
